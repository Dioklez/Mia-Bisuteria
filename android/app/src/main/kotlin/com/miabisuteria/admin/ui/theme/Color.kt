package com.miabisuteri.admin.ui.theme

import androidx.compose.ui.graphics.Color

// ─── Brand greens (accent only — not backgrounds) ───────────────────────────
val VerdeOscuro = Color(0xFF1e2d08)
val VerdeAcento = Color(0xFF4a6b22)
val VerdeClaro = Color(0xFF7a9c52)
val VerdeMenta = Color(0xFFc8deb0)

// ─── Dorado ──────────────────────────────────────────────────────────────────
val Oro = Color(0xFFb8862e)
val Oro2 = Color(0xFFd4a84a)

// ─── Admin dark surfaces (neutral charcoal — not green) ─────────────────────
val AdminBackground = Color(0xFF0D0D0D)   // fondo general
val AdminSurface   = Color(0xFF161616)    // scaffold / topbar
val AdminSurface2  = Color(0xFF1F1F1F)   // tab row / secondary surface
val AdminCard      = Color(0xFF242424)    // cards
val AdminBorder    = Color(0xFF383838)    // bordes sutiles
val AdminDivider   = Color(0xFF2C2C2C)   // divisores

// ─── Texto ───────────────────────────────────────────────────────────────────
val TextoPrimario   = Color(0xFFF0F0F0)
val TextoSecundario = Color(0xFFAAAAAA)
val TextoTerciario  = Color(0xFF666666)

// ─── Estados de pedido ───────────────────────────────────────────────────────
val EstadoNuevo      = Color(0xFF4A90D9)
val EstadoEnProceso  = Color(0xFFE07C3A)
val EstadoListo      = Color(0xFF5DBD6E)
val EstadoCancelado  = Color(0xFFD05050)
